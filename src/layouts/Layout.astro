---
import { pwaInfo } from "virtual:pwa-info";
import Icon from "astro-icon";
import socialLinksData from "../data/socialLinks.ts";
import { type SocialLink } from "../types/SocialLink.ts";
import ReloadPrompt from "../components/ReloadPrompt.astro";
import deliverable from "../data/deliverable";
import JsonLdConceptArt from "../components/JsonLdConceptArt.astro";
import type { ElementMetadata } from "../types/ElementMetadata";
---

<!doctype html>
<html lang="en">
  <head>
    <!-- HTML Meta Tags -->
    <meta charset="UTF-8" />
    <title>Ronnie Nimer</title>
    <meta name="viewport" content="width=device-width" />
    <meta name="theme-color" content="#222222" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content="Character Concept Artist Portfolio" />
    <!-- Facebook Meta Tags -->
    <meta property="og:url" content="https://ronnienimer.art/" />
    <meta property="og:type" content="website" />
    <meta property="og:title" content="Ronnie Nimer" />
    <meta
      property="og:description"
      content="Character Concept Artist Portfolio"
    />
    <meta
      property="og:image"
      content="https://ik.imagekit.io/8hod93w55/social-metadata-image.jpg?tr=w-500,f-webp"
    />
    <!-- Twitter Meta Tags -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta property="twitter:domain" content="ronnienimer.art" />
    <meta property="twitter:url" content="https://ronnienimer.art/" />
    <meta name="twitter:title" content="Ronnie Nimer" />
    <meta
      name="twitter:description"
      content="Character Concept Artist Portfolio"
    />
    <meta
      name="twitter:image"
      content="https://ik.imagekit.io/8hod93w55/social-metadata-image.jpg?tr=w-500,f-webp"
    />
    <!-- Icons -->
    <link rel="icon" href="/favicon.ico" sizes="any" />
    <link rel="apple-touch-icon" href="/apple-touch-icon-180x180.png" />
    <!-- Fonts -->
    <link
      href="https://fonts.googleapis.com/css?family=Inter&display=swap"
      rel="preload"
      as="style"
    />
    <link
      href="https://fonts.googleapis.com/css?family=Inter&display=swap"
      rel="stylesheet"
    />
    <!-- Structured Data -->
    {deliverable.map((data) => <JsonLdConceptArt {...data} />)}
    <!-- PWA -->
    {pwaInfo && <Fragment set:html={pwaInfo.webManifest.linkTag} />}
  </head>
  <body>
    <slot name="background" />
    <header>
      <div class="profile">
        <img
          src="https://ik.imagekit.io/8hod93w55/tr:w-262,h-262,q-100/ronnie-profile.jpg"
          alt="Ronnie Nimer"
          width="55px"
          height="55px"
          fetchpriority="high"
          loading="eager"
          decoding="auto"
        />
      </div>

      <div class="artist">
        <p class="fullname">Ronnie Nimer</p>
        <p class="title">Concept Artist</p>
      </div>

      <nav aria-label="header social links">
        {
          socialLinksData.map(({ href, title, icon }: SocialLink) => (
            <a href={href} title={title} class="socialLink" target="_blank">
              <Icon
                pack={icon.pack}
                name={icon.name}
                color={icon.color ?? "#61cdff"}
              />
            </a>
          ))
        }
      </nav>
    </header>
    <slot name="main" />
    <footer>
      <hr />
      <h3>
        <b>CONTACT ME</b>
      </h3>
      <h4>Ronnie Nimer</h4>
      <h5>Concept Artist</h5>
      <p>
        <a href="mailto:art.ronnie29@gmail.com" title="email address"
          >art.ronnie29@gmail.com</a
        >
      </p>
      <nav aria-label="footer social links">
        {
          socialLinksData.map(({ href, title, icon }: SocialLink) => (
            <a href={href} title={title} class="socialLink" target="_blank">
              <Icon
                pack={icon.pack}
                name={icon.name}
                color={icon.color ?? "#ffffff"}
              />
            </a>
          ))
        }
      </nav>
    </footer>
    <ReloadPrompt />
  </body>
</html>

<style is:global>
  * {
    box-sizing: border-box !important;
  }
  html {
    font-size: 16px;
    font-family: Inter, sans-serif;
  }
  @media (max-width: 1080px) {
    html {
      font-size: 14px;
    }
  }
  body {
    margin: 0;
    width: 100%;
    background: #222222;
    position: relative;
  }
  header {
    display: flex;
    width: 100%;
    max-width: 1025px;
    margin: 0 auto;
    position: relative;
    align-items: center;
    padding: 1.5rem 1.25rem;
    gap: 0.75rem;
    z-index: 1;
  }
  header .profile {
    border-radius: 50%;
    overflow: hidden;
    width: 55px;
    height: 55px;
  }
  header .artist {
    flex-grow: 1;
  }
  header .artist .fullname {
    font-family: Inter, sans-serif;
    font-size: 1.25rem;
    font-weight: 600;
    color: #fff;
    margin: 0;
  }
  header .artist .title {
    font-family: Inter, sans-serif;
    font-size: 1rem;
    font-weight: 400;
    color: #aaa;
    margin: 0;
  }
  header .links a {
    color: #fff;
    font-family: Inter, sans-serif;
  }
  header nav,
  footer nav {
    display: flex;
    gap: 1rem;
  }
  footer {
    display: flex;
    flex-direction: column;
    align-items: center;
    width: 100%;
    max-width: 1025px;
    margin: 0 auto;
    gap: 1rem;
    padding: 0 2rem 2.5rem;
    z-index: 1;
  }
  footer hr {
    width: 100%;
    height: 1px;
    background: #ffffff66;
    margin: 0 0 1.5rem;
    border: none;
  }
  footer p,
  footer h3,
  footer h4,
  footer h5 {
    margin: 0;
    font-weight: 400;
    color: #ffffff;
  }
  footer h5 {
    color: #bbbbbb;
  }
  footer a {
    margin: 0;
    color: #61cdff;
  }
  footer .socialLink svg {
    transition: color ease-in-out 0.2s;
  }
  .socialLink {
    display: inline-block;
    width: 2rem;
    height: 2rem;
  }
  .socialLink svg {
    transition: color ease-in-out 0.2s;
  }
  .socialLink:hover svg {
    color: #eeeeee;
  }
</style>
