---
import socialLinksData from "../../data/socialLinks";
import SocialLink from "../core/SocialLink.astro";
---

<footer>
  <p>
    <b>CONTACT ME</b>
  </p>
  <p>
    <a href="mailto:art.ronnie29@gmail.com" title="email address">
      art.ronnie29@gmail.com
    </a>
  </p>
  <nav aria-label="footer social links">
    {
      socialLinksData.map((props) => (
        <SocialLink {...props} ariaLabel={`Footer link to ${props.title}`} />
      ))
    }
  </nav>
</footer>

<style>
  footer {
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
    width: 100%;
    max-width: var(--layoutMaxWidth);
    margin: 0 auto;
    gap: 1rem;
    padding: 0 2rem 2.5rem;
    z-index: 1;
  }
  @media (max-width: 480px) {
    footer {
      flex-direction: column;
    }
  }
  footer hr {
    width: 100%;
    height: 1px;
    background: rgba(var(--foregroundColor), 50%);
    margin: 0 0 1.5rem;
    border: none;
  }
  footer p {
    font-size: initial;
    margin: 0;
    font-weight: 400;
    color: rgb(var(--foregroundColor));
    text-align: center;
  }
  footer a {
    margin: 0;
    color: rgb(var(--primaryColor));
  }
  footer nav {
    display: flex;
    gap: 1rem;
  }
</style>
